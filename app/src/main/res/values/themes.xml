<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RefreshPaper" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
<!--        <item name="colorPrimary">@color/white</item>-->
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="materialSwitchStyle">@style/AppTheme.Switch</item>
    </style>

    <!-- ActionBar style -->
    <style name="AppTheme.ActionBar" parent="Widget.Material3.ActionBar.Solid">
        <item name="background">@android:color/white</item>
        <item name="titleTextColor">@android:color/black</item>
    </style>

    <!-- Custom Button Style -->
    <style name="CustomButton" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@android:color/white</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <!-- Custom Switch Style -->
    <style name="AppTheme.Switch" parent="Widget.Material3.CompoundButton.MaterialSwitch">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Switch</item>
    </style>

    <style name="ThemeOverlay.App.Switch" parent="">
        <item name="colorPrimary">@android:color/darker_gray</item>
        <item name="colorSurfaceVariant">@android:color/white</item>
    </style>



    <style name="Theme.RefreshPaper" parent="Base.Theme.RefreshPaper" />
</resources>